@name drag by M0SKAU
@inputs [E1 E2 E3 E4]:entity

if(first() | dupefinished()) {
    
local Drag = 1
    
local InertiaX=10
local InertiaY=12
local InertiaZ=10

#######    CODE BARRIER    #######

    # Parent
    #entity():setAlpha(255)

    E1:propInertia(vec(InertiaX, InertiaY, InertiaZ))
    E2:propInertia(vec(InertiaX, InertiaY, InertiaZ))
    E3:propInertia(vec(InertiaX, InertiaY, InertiaZ))
    E4:propInertia(vec(InertiaX, InertiaY, InertiaZ))

    E1:propDrag(Drag)
    E2:propDrag(Drag)
    E3:propDrag(Drag)
    E4:propDrag(Drag)
    
    E1:propPhysicalMaterial("jalopytire")
    E2:propPhysicalMaterial("jalopytire")
    E3:propPhysicalMaterial("jalopytire")
    E4:propPhysicalMaterial("jalopytire")
}
